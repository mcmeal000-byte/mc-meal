# Hotfix – Craft House + Soda Tray

Fixed:

- Craft House crash caused by missing recipe helper functions.
- Soda Sprint order tray text no longer overflows outside the tray box.
- Visible app strings are kept in English.
- The TronLink `tronlinkParams` console error is caused by a browser extension injection, not by MC Meal app code.
